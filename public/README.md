# Public Assets Directory

Place any static assets here that should be served directly by Next.js.

This could include:
- Favicons (favicon.ico)
- Images
- Documents
- Other static files

Files in this directory are served at the root URL.
Example: `/public/favicon.ico` → `http://localhost:3000/favicon.ico`
